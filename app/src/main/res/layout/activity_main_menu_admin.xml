<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    style="@style/ActivityTheme">

    <TextView
        android:layout_weight="1"
        android:text="@string/menuPrincipalAdmin"
        style="@style/Title"
        android:layout_width="match_parent"
        android:layout_height="@dimen/zero" />

    <include layout="@layout/layout_main_menu" />

    <Button
        android:id="@+id/btnContas"
        android:layout_weight="1"
        android:text="@string/registarContas"
        android:textColor="@color/textColorLighter"
        android:layout_margin="@dimen/btn_margin_higher"
        android:background="@color/darkRed"
        android:layout_width="match_parent"
        android:layout_height="@dimen/zero" />

</LinearLayout>